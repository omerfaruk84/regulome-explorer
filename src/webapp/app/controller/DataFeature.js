Ext.define('RE.controller.DataFeature', {
    extend: 'Ext.app.Controller',

    init: function() {
        console.log('Initialized DataFeature! This happens before the Application launch function is called');
    }
});